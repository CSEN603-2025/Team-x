import React from "react";
import ReactDOM from "react-dom/client";
import SCADOfficeApp from "./SCADOfficeApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SCADOfficeApp />
  </React.StrictMode>
);
